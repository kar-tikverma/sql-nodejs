<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication</title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="stylesheet" href="/password.css" />
</head>
<body>
    <form action="http://localhost:<%= port %>/users/<%= id %>/authenticate?action=<%= action %>" method="post">
        <div class="pass">
            <label for="password">Password:</label>
            <div class="inpass">
                <input id="password" class="password" name="password" type="password">
                <p id="passMessage" class="message">Incorrect Password!</p>
            </div>
        </div>
        <div class="check">
            <input type="checkbox" id="checkbox">
            <label for="checkbox">Show Password</label>
        </div>
        <button>Next</button>
    </form>
    <script>
        const incorrectPass = <%= incorrectPass %>;
    </script>
    <script src="/password.js"></script>
</body>
</html>